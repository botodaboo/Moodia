{{#each article}}
<div class="card">
    <div class="card-header">
        <h3>{{title}}</h3>
    </div>
    <div class="card-content">
        <p class="ml-4">{{content}}</p>
    </div>
</div>
{{/each}}